# The project will be run on a randomly selected simulator for any of the devices that support iOS 13.6.
platform :ios, '13.0'

target 'venturus-ios' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  
  # Pods for venturus-ios
  pod 'RxSwift'
  pod 'RxCocoa'
  pod 'Nuke'
end
